# Normalize distance matrix so maximum distance is 1

Normalize distance matrix so maximum distance is 1

## Usage

``` r
normalize_distances(distmat)
```

## Arguments

- distmat:

  n x n distance matrix

## Value

Normalized distance matrix
