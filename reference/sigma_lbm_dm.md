# Compute demeaned LBM covariance matrix

sigma_lbm_dm = demeanmat(sigma_lbm(distmat))

## Usage

``` r
sigma_lbm_dm(distmat)
```

## Arguments

- distmat:

  n x n distance matrix

## Value

Double-centered LBM covariance matrix
