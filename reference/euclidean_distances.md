# Compute Euclidean distance matrix

Compute Euclidean distance matrix

## Usage

``` r
euclidean_distances(coords)
```

## Arguments

- coords:

  n x d matrix of coordinates

## Value

n x n Euclidean distance matrix
